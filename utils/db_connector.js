
// const pgp = require('pg-promise')({
//     noWarnings: true
// })

// const db = pgp(`postgres://${process.env.DB_USERNAME}:${process.env.DB_PASSWORD}@${process.env.HOST}:${process.env.DB_PORT}/${process.env.DB_DATABASE}`);

// export default db;